const voteOption = document.getElementById('vote-<%= @post_vote.post_option.id %>');
voteOption.insertAdjacentHTML('afterbegin', '<%= @post_vote.post_option.post_votes.count %>');

const indexBody = document.getElementById('index-container');
const div = document.createElement("div");


<% if @post_vote.errors.any? %>
  indexBody.insertAdjacentHTML('beforeend', )
<% else %>

<% end %>
